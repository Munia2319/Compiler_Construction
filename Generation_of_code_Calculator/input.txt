Algorithm name-CalculatorScript
Declaration block- float t1=10, t2=2;

start{
    sum= t1+t2+t1;
    product=t1*t2;
    quotient=t1/t2;
    complexCalculation=(sum*product)/quotient;
    show(" Sum: " + sum);
    show(" Product: " + product);
    show(" Quotient: " + quotient);
    show(" Complex Calculation Result: " + complexCalculation);
}
end
